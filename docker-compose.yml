version: '3.8'

services:
  alumni-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGO_URI=mongodb+srv://rveducational_db_user:Alumni%40123@cluster0.xk6n9j6.mongodb.net/?appName=Cluster0
      - JWT_SECRET=super_secret_jwt_key_rvce_alumni_2026_xyz
      - FCM_SERVER_KEY=AIzaSyAqq3YEanICCnbifvSkwnuHH6jzPtW7c-g
    restart: always

  nginx-proxy:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - alumni-backend
    restart: always
